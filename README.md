# kistie-store

This is a simple static project generated from an existing template.

Files:
- `index.html` — main site page

Quick local preview (PowerShell):

```powershell
# serve current folder on port 8000 (requires Python 3)
Set-Location -Path "c:\Nalumansi\kistie-store"; python -m http.server 8000
```

Open http://localhost:8000 in your browser.
